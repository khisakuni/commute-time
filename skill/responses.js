// TODO: use Address object to get proper response
const inquireStartAddress = () => (
  'Tell me the building address of where you\'re starting from. For example, say one two three four'
);

module.exports = {
  inquireStartAddress,
};
